import { RegularTransaction } from './regular-transaction';

describe('RegularTransaction', () => {
  it('should create an instance', () => {
    expect(new RegularTransaction()).toBeTruthy();
  });
});
